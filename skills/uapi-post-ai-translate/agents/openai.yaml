interface:
  display_name: "UAPI AI智能翻译 接口"
  short_description: "使用 UAPI 的AI智能翻译接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-post-ai-translate 直接调用 POST /ai/translate，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
