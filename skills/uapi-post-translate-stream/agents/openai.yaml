interface:
  display_name: "UAPI 流式翻译（中英互译） 接口"
  short_description: "使用 UAPI 的流式翻译（中英互译）接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-post-translate-stream 直接调用 POST /translate/stream，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
