interface:
  display_name: "UAPI 执行DNS解析查询 接口"
  short_description: "使用 UAPI 的执行DNS解析查询接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-network-dns 直接调用 GET /network/dns，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
