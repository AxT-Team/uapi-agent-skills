interface:
  display_name: "UAPI 转换时间戳 (旧版，推荐使用/convert/unixtime) 接口"
  short_description: "使用 UAPI 的转换时间戳 (旧版，推荐使用/convert/unixtime)接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-misc-timestamp 直接调用 GET /misc/timestamp，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
