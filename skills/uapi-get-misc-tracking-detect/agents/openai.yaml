interface:
  display_name: "UAPI 识别快递公司 接口"
  short_description: "使用 UAPI 的识别快递公司接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-misc-tracking-detect 直接调用 GET /misc/tracking/detect，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
