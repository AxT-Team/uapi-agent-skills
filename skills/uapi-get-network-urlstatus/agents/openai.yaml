interface:
  display_name: "UAPI 检查URL的可访问性状态 接口"
  short_description: "使用 UAPI 的检查URL的可访问性状态接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-network-urlstatus 直接调用 GET /network/urlstatus，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
