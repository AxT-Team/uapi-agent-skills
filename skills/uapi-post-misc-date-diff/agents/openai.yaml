interface:
  display_name: "UAPI 计算两个日期之间的时间差值 接口"
  short_description: "使用 UAPI 的计算两个日期之间的时间差值接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-post-misc-date-diff 直接调用 POST /misc/date-diff，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
