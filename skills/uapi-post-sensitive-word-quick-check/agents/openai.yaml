interface:
  display_name: "UAPI 敏感词检测（快速） 接口"
  short_description: "使用 UAPI 的敏感词检测（快速）接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-post-sensitive-word-quick-check 直接调用 POST /text/profanitycheck，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
