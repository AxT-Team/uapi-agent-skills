interface:
  display_name: "UAPI Ping 主机 接口"
  short_description: "使用 UAPI 的Ping 主机接口，并说明参数、调用方式和返回码"
  default_prompt: "使用 $uapi-get-network-ping 直接调用 GET /network/ping，并先确认必填参数、默认值和生效条件。"
policy:
  allow_implicit_invocation: true
